# Database
DATABASE_URL="postgresql://user:password@localhost:5432/cherkashynapiano"

# JWT Secret (generate a strong random string for production)
JWT_SECRET="your-secret-key-change-in-production"

# Admin Setup Key (use only once during initial setup)
ADMIN_SETUP_KEY="setup-key-change-after-first-use"

# Strapi API URL (for fetching public data if needed)
NEXT_PUBLIC_STRAPI_URL="http://localhost:1337"
NEXT_PUBLIC_STRAPI_API_URL="http://localhost:1337"

# Node Environment
NODE_ENV="development"
